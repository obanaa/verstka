class_name: AcceptanceTester
modules:
    enabled:
        - WebDriver
        - Helper\Acceptance


    config:
        WebDriver:
            url: 'http://itsvit.com'

            browser: chrome
            window_size: 1980x1780
            restart: true

env:

  firefox:
    modules:
      config:
        WebDriver:
          url: 'http://itsvit.com'
          restart: false
          browser: firefox
          window_size: 1980x1780



env:

  phantom:
    modules:
      config:
        WebDriver:
          url: 'http://itsvit.com'
          restart: false
          browser: phantomjs
          window_size: 1980x1780
